# Configuration for login attempt management
dir = /var/run/faillock

# Number of failed attempts before locking
deny = 5

# Time window for counting failed attempts (5 minutes)
fail_interval = 300

# Lock duration (1 minute)
unlock_time = 60
