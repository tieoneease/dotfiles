# Vial-specific rule — matches keyboards with Vial firmware via magic serial.
# More restrictive than the broad hidraw rule in 50-qmk.rules.
KERNEL=="hidraw*", SUBSYSTEM=="hidraw", ATTRS{serial}=="*vial:f64c2b3c*", MODE="0660", GROUP="users", TAG+="uaccess"
