[Service]
# Work around hidraw FD leak in asusd (asusctl 6.3.2).
# Without this, asusd exhausts the 64-minor hidraw pool after ~15 reconnects,
# breaking WebHID (VIA/Vial) and any app that needs /dev/hidraw*.
RuntimeMaxSec=86400
