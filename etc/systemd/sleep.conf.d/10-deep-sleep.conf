[Sleep]
# Prefer S3 (deep) suspend over s2idle (modern standby).
# s2idle keeps the CPU in a shallow sleep state and drains battery
# significantly faster, especially on ASUS laptops.
# Verify with: cat /sys/power/mem_sleep (should show "s2idle [deep]")
MemorySleepMode=deep
