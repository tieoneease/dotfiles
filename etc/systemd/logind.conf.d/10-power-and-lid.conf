[Login]
# Power button suspends instead of powering off (systemd default is poweroff).
# logind properly debounces the wake-from-suspend key event, unlike niri which
# re-suspends immediately (niri issue #2233).
HandlePowerKey=suspend

# Power button always works, even when a keep-awake inhibitor is active.
PowerKeyIgnoreInhibited=yes
